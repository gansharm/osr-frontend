# Exhibition videos

Add optimized MP4 or WebM files to this folder, import them in `src/data/exhibitionData.js`, and assign each import to a video item's `src` field.

Keep a separate compressed image in the images folder for each video's `thumbnail`.
