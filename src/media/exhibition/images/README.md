# Exhibition images

Add exhibition images to this folder, import them in `src/data/exhibitionData.js`, and update `heroImage` or `galleryImages` there. Components do not need to be changed.

Recommended: JPEG or WebP, at least 1200px wide, compressed for the web.
